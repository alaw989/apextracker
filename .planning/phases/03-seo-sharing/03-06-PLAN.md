---
phase: 03-seo-sharing
plan: 06
type: execute
wave: 3
depends_on:
  - 03-03
files_modified:
  - src/views/PlayerView.vue
  - src/components/stats/PlayerHeader.vue
  - src/components/legends/LegendCard.vue
  - src/components/search/SearchInput.vue
  - src/components/search/PlatformSelect.vue
  - src/components/search/SearchButton.vue
  - src/components/stats/StatsList.vue
  - src/components/legends/FavoriteLegends.vue
autonomous: true

must_haves:
  truths:
    - "Player profile uses structured data markup (schema.org/Person)"
    - "Images have alt text for accessibility"
    - "Headings use proper h1-h6 hierarchy"
    - "Interactive elements have ARIA labels"
  artifacts:
    - path: "src/views/PlayerView.vue"
      provides: "Player profile with Schema.org markup"
      contains: "itemscope, itemtype, itemprop"
    - path: "src/components/stats/PlayerHeader.vue"
      provides: "Player header with alt text"
      contains: "alt attribute"
    - path: "src/components/legends/LegendCard.vue"
      provides: "Legend card with alt text"
      contains: "alt attribute"
  key_links:
    - from: "src/views/PlayerView.vue"
      to: "schema.org/Person"
      via: "itemscope itemtype attribute"
      pattern: "itemscope.*itemtype"
---

<objective>
Add Schema.org structured data and complete accessibility improvements.

Purpose: Build on semantic HTML from plan 03-03 by adding Schema.org markup for search engines and accessibility features (alt text, ARIA labels) for screen readers.

Output: Player profile includes Schema.org Person markup, images have alt text, interactive elements have ARIA labels.
</objective>

<execution_context>
@/home/deck/.claude/get-shit-done/workflows/execute-plan.md
@/home/deck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-seo-sharing/03-CONTEXT.md
@.planning/phases/03-seo-sharing/03-RESEARCH.md

# Phase 3 Context
@.planning/phases/03-seo-sharing/03-03-SUMMARY.md

# Existing code
@src/views/PlayerView.vue
@src/components/stats/PlayerHeader.vue
@src/components/legends/LegendCard.vue
@src/components/search/SearchInput.vue
@src/components/search/PlatformSelect.vue
@src/components/search/SearchButton.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Schema.org markup to PlayerView</name>
  <files>src/views/PlayerView.vue</files>
  <action>
    Update PlayerView.vue with Schema.org structured data on top of existing semantic HTML.

    Add to the existing <article> element:
    1. Add itemscope itemtype="https://schema.org/Person" to article
    2. Add itemprop="name" to player name h1
    3. Add itemprop="image" to avatar img
    4. Add itemprop="affiliation" for platform

    Schema.org markup:
    ```html
    <article itemscope itemtype="https://schema.org/Person">
      <header>
        <h1 itemprop="name">{{ player.name }}</h1>
        <img itemprop="image" :src="player.avatar" :alt="`${player.name} avatar`" />
        <span itemprop="affiliation">{{ platformName }}</span>
      </header>

      <section aria-labelledby="stats-heading">
        <h2 id="stats-heading" class="visually-hidden">Player Statistics</h2>
        <!-- stats content -->
      </section>
    </article>
    ```

    DO NOT modify the existing semantic HTML structure from plan 03-03
    DO NOT add itemsprop on decorative elements
  </action>
  <verify>
    PlayerView.vue contains:
    - <article> with itemscope itemtype="https://schema.org/Person"
    - <h1> with itemprop="name"
    - <img> with itemprop="image"
    - Platform info with itemprop="affiliation"
  </verify>
  <done>
    PlayerView has Schema.org Person structured data markup
  </done>
</task>

<task type="auto">
  <name>Task 2: Add alt text to images in components</name>
  <files>src/components/stats/PlayerHeader.vue, src/components/legends/LegendCard.vue</files>
  <action>
    Ensure all images have descriptive alt text for accessibility and SEO.

    PlayerHeader.vue changes:
    1. Add dynamic alt text to avatar image
    2. Add alt text to rank icon image
    3. Alt format: "{PlayerName} avatar", "{RankName} rank icon"

    Example:
    ```html
    <img :src="player.avatar" :alt="`${player.name} avatar`" />
    <img :src="player.rankIcon" :alt="`${player.rank} rank icon`" />
    ```

    LegendCard.vue changes:
    1. Add alt text for legend images
    2. Alt format: "{LegendName} legend art"

    DO NOT use empty alt="" unless image is purely decorative
    DO NOT use generic alt like "image" or "icon"
  </action>
  <verify>
    PlayerHeader.vue contains:
    - Avatar img with dynamic alt text including player name
    - Rank icon img with alt text describing rank

    LegendCard.vue contains:
    - Legend img with alt text including legend name
  </verify>
  <done>
    All images have descriptive alt text
  </done>
</task>

<task type="auto">
  <name>Task 3: Add ARIA labels to interactive elements</name>
  <files>src/components/search/SearchInput.vue, src/components/search/PlatformSelect.vue, src/components/search/SearchButton.vue</files>
  <action>
    Add ARIA labels to form controls for screen reader accessibility.

    SearchInput.vue changes:
    1. Add aria-label to input element
    2. Use existing id and label relationship if present
    3. ARIA label: "Player username search"

    PlatformSelect.vue changes:
    1. Add aria-label to select element
    2. ARIA label: "Select platform"

    SearchButton.vue changes:
    1. Add aria-label to button if text is not descriptive enough
    2. Add aria-busy="true" when loading state is active
    3. ARIA label updates: "Search for player", "Searching..."

    DO NOT add redundant ARIA labels when visible label is sufficient
    DO add aria-live for error messages if not already present
  </action>
  <verify>
    Search components contain:
    - ARIA labels on form controls
    - aria-busy on button during loading
    - Proper association between labels and inputs
  </verify>
  <done>
    All interactive elements have appropriate ARIA labels
  </done>
</task>

</tasks>

<verification>
After all tasks complete, verify:
- [ ] PlayerView has Schema.org Person markup
- [ ] All images have descriptive alt text
- [ ] Form controls have ARIA labels
- [ ] Loading states use aria-busy
</verification>

<success_criteria>
1. Player profile includes Schema.org Person structured data
2. Images have descriptive alt text for accessibility
3. Form controls have ARIA labels
4. Loading states communicate properly to assistive technology
</success_criteria>

<output>
After completion, create `.planning/phases/03-seo-sharing/03-06-SUMMARY.md`
</output>
